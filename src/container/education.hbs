<div>
    {{#with main}}
    <main>
        <section>
            {{> title data="My educationss" big=true}}
            <ul class="ps-5">
                {{#each education}}
                <li>
                    {{name}}
                    {{#if isEnd}} (+) {{/if}}

                    {{#unless isEnd}} (ще в процесі) {{/unless}}
                </li>
                {{/each}}
            </ul>
        </section>

        <section>
            {{> title data="My certificates"}}
            <ul class="ps-5">
                {{#each certificates}}
                <li>
                    {{name}}
                    {{#if id}} ({{id}}) {{/if}}
                </li>
                {{/each}}
            </ul>
        </section>
    </main>
    {{/with}}
</div>